
const Wrapper= props =>{
    return props.children; //children props holds all the content u are passing between the opening and closing tag of ur custom component ,
    // this is mt wrapper
};

export default Wrapper;